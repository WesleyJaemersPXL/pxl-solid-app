const authService = {
  /**
   * Implement this function so that you can login!
   *
   * Hint: look at the documentation (there are a lot of good examples on how to login)
   *
   * @param {*} oidcIssuer    the iri to the oidcIssuer you wish to use
   */
  login: async (oidcIssuer) => {
    throw new Error(
      "Not implemented yet! Implement 'login' at 'AuthService.js'"
    );
  },
};

export default authService;
